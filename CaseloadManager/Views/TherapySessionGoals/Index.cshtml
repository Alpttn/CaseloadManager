@model IEnumerable<CaseloadManager.Models.TherapySessionGoal>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Goal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TherapySession)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Goal.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TherapySession.Note)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TherapySessionGoalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TherapySessionGoalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TherapySessionGoalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
